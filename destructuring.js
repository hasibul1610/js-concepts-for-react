//Array Destructuring
const [x,y] = [5,6];

// console.log(x,y);

// Object Destructuring 
const products = {
    name: 'Shanto',
    designation: 'developer',
    language: ['html', 'css', 'Javascript', 'react', 'node'],
};

const {name,designation,language} = products;

// console.log(name,designation,language[2]);

